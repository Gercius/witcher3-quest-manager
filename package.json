{
    "name": "witcher3-quest-order",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "concurrently \"vite\" \"npm run css\"",
        "build": "tsc && vite build --base=./",
        "preview": "vite preview",
        "css": "sass --watch src/styles/sass:src/styles/css"
    },
    "devDependencies": {
        "concurrently": "^9.1.0",
        "exceljs": "^4.4.0",
        "sass": "^1.81.0",
        "typescript": "~5.6.2",
        "vite": "^5.4.10"
    }
}
